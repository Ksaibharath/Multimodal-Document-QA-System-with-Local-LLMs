# Multimodal-Document-QA-System-with-Local-LLMs
Built a Multimodal PDF RAG Chatbot that allows users to upload PDF documents and ask natural language questions. The system extracts document text, applies OCR for scanned pages, renders page images for visual context, stores embeddings in a vector database, retrieves relevant chunks, and generates answers locally using Ollama.
